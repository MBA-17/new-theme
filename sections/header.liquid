{{ 'header.css' | asset_url | stylesheet_tag }}



<header class="site-header" role="banner" style="--header-bg: {{settings.background_color}}; --header-foreground: {{ settings.color_foreground }};">
  <div class="container header__container">

    <!-- Store name -->
    <h1 class="site-title" style="max-width: {{ section.settings.logo_max_width }}px;">
      <a href="{{ routes.root_url }}" class="site-title__link" style="display: flex; align-items: center;">
    
        {%- if section.settings.logo_image != blank -%}
          <img
            src="{{ section.settings.logo_image | img_url: 'medium' }}"
            class="site-logo desktop-logo"
            alt="{{ shop.name | escape }}"
            width="{{ section.settings.logo_max_width }}"
            height="auto"
            loading="lazy"
          >
        {%- endif -%}

        {%- if section.settings.mobile_logo_image != blank -%}
          <img
            src="{{ section.settings.mobile_logo_image | img_url: 'medium' }}"
            class="site-logo mobile-logo"
            alt="{{ shop.name | escape }}"
            width="{{ section.settings.logo_max_width }}"
            height="auto"
            loading="lazy"
          >
        {%- endif -%}
 
        {%- if section.settings.dark_logo_image != blank -%}
          <img
            src="{{ section.settings.dark_logo_image | img_url: 'medium' }}"
            class="site-logo dark-logo"
            alt="{{ shop.name | escape }}"
            width="{{ section.settings.logo_max_width }}"
            height="auto"
            loading="lazy"
          >
        {%- endif -%}

        {%- unless section.settings.logo_image or section.settings.mobile_logo_image -%}
          <span class="site-logo--text">{{ shop.name }}</span>
        {%- endunless -%}

      </a>
    </h1>

    <!-- Mobile-Menu-icon -->
    <div class="mobile-menu-icon">
      <button class="menu-icon" type="button" id="mobile-menu-toggle" aria-label="Open mobile menu" aria-expanded="false">
        {% render 'Menu-icon' %}
      </button>
    </div>

    <!-- Nav Menus -->
    <nav class="site-nav" role="navigation" aria-label="Main menu">
      <ul class="nav__list">
        {% for link in section.settings.menu.links %}
          <li class="nav__item {% if link.links != blank %}has-dropdown{% endif %}">
            {% if link.links != blank %}
              <button
                class="dropdown-toggle"
                aria-haspopup="true"
                aria-expanded="false"
                aria-controls="submenu-{{ forloop.index }}"
                data-dropdown-toggle
              >
                {{ link.title }}
                <svg class="dropdown-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="6 9 12 15 18 9"></polyline>
                </svg>
              </button>
              <ul class="dropdown-menu" id="submenu-{{ forloop.index }}" role="menu" aria-hidden="true">
                {% for child_link in link.links %}
                  <li role="none">
                    <a href="{{ child_link.url }}" role="menuitem" class="dropdown__link">
                      {{ child_link.title }}
                    </a>
                  </li>
                {% endfor %}
              </ul>
            {% else %}
              <a href="{{ link.url }}" class="nav__link">{{ link.title }}</a>
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    </nav>

    <!-- Account & Cart -->
    <div class="header__utilities">
      {% if shop.customer_accounts_enabled %}
        <div class="account-links">
          {% if customer %}
            <a href="{{ routes.account_url }}" class="account-link" aria-label="My account">
              {{ 'icon-account.svg' | inline_asset_content }}
            </a>
            {{ 'Logout' | customer_logout_link | replace: '<a', '<a class="account-link logout-link"' }}
          {% else %}
            {{ 'Login' | customer_login_link | replace: '<a', '<a class="account-link login-link"' }}
            {{ 'Register' | customer_register_link | replace: '<a', '<a class="account-link register-link"' }}
          {% endif %}
        </div>
      {% endif %}

      <a href="{{ routes.cart_url }}" class="cart-link" aria-label="Shopping cart">
        {% if cart.item_count > 0 %}
          <span class="cart-count" aria-live="polite">{{ cart.item_count }}</span>
        {% endif %}
        {{ 'icon-cart.svg' | inline_asset_content }}
      </a>
    </div>

   <!-- Mobile-Menu -->
    <div class="mobile-menu-container" id="mobile-menu-container">
      <div class="mobile-menu-overlay" id="mobile-overlay"></div>
        <div class="mobile-menu-drawer">
           <!-- Mobile-Close-Button -->
          <div class="close">
          <button class="close-btn" type="button" id="mobile-menu-close" aria-label="Close mobile menu">
            {% render 'close-button' %}
          </button>
        </div>

        <!-- Mobile-Navigation -->
        <nav class="mobile-nav" role="navigation" aria-label="Mobile menu">
          <ul class="mobile_nav__list">
            {% for link in section.settings.menu.links %}
              <li class="mobile_nav__item {% if link.links != blank %}has-dropdown{% endif %}">
                {% if link.links != blank %}
                  <button
                    class="mobile-dropdown-toggle"
                    aria-haspopup="true"
                    aria-expanded="false"
                    aria-controls="mobile-submenu-{{ forloop.index }}"
                  >
                    {{ link.title }}
                    <svg class="mobile-dropdown-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                  <ul class="mobile-dropdown-menu" id="mobile-submenu-{{ forloop.index }}" role="menu" aria-hidden="true">
                    {% for child_link in link.links %}
                      <li role="none">
                        <a href="{{ child_link.url }}" role="menuitem" class="mobile__dropdown__link">
                          {{ child_link.title }}
                        </a>
                      </li>
                    {% endfor %}
                  </ul>
                {% else %}
                  <a href="{{ link.url }}" class="mobile__nav__link">{{ link.title }}</a>
                {% endif %}
              </li>
            {% endfor %}
          </ul>
        </nav>

        <!-- Mobile-account-login -->
        <div class="mobile-account-links">
          {% if customer %}
            <a href="{{ routes.account_url }}" class="account-link" aria-label="My account">
              {{ 'icon-account.svg' | inline_asset_content }}
            </a>
            {{ 'Logout' | customer_logout_link | replace: '<a', '<a class="account-link logout-link"' }}
          {% else %}
            {{ 'Login' | customer_login_link | replace: '<a', '<a class="account-link login-link"' }}
            {{ 'Register' | customer_register_link | replace: '<a', '<a class="account-link register-link"' }}
          {% endif %}
        </div>
      </div>
    </div>

  </div>
</header>

<script src="{{ 'header.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "t:general.header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Desktop Logo"
    },
    {
      "type": "image_picker",
      "id": "mobile_logo_image",
      "label": "Mobile Logo"
    },
    {
      "type": "image_picker",
      "id": "dark_logo_image",
      "label": "Dark Mode Logo"
    },
    {
      "type": "range",
      "id": "logo_max_width",
      "label": "Max logo width (PX)",
      "min": 100,
      "max": 400,
      "step": 10,
      "default": 200
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:labels.menu"
    }
  ]
}
{% endschema %}